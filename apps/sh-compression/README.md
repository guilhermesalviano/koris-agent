# sh-compression

This is a sh compression hook, and can be used directly in terminal.

## Development

From repo root:

```bash
pnpm lint
pnpm test
pnpm build
```

## Usage

```sh
node apps/sh-compression/dist/src/cli.js ls
```

## Hooks
***